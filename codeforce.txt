#include<iostream>
#include<algorithm>
#include<queue>
#include<string>

#define all(x) x.begin(), x.end()
#define sz(x) x.size()
#define lng(x) x.length()

using namespace std; 

typedef long long ll; 
typedef double db; 
typedef pair<int, int> pll; 

int main() {
	ios::sync_with_stdio(false); 
	cin.tie(NULL); cout.tie(NULL); 
	char a[200001], b[200001];
	int a1, b1; int star = 0; 
	cin >> a1 >> b1; 
	bool res = true; 
	for (int i = 0; i < a1; i++) {
		cin >> a[i]; 
		if (a[i] == '*') {
			res = true; 
		}
	}
	for (int i = 0; i < b1; i++) {
		cin >> b[i]; 
		if (b[i] == true) {
			res = false; 
		}
	}
	if (res) {  // a¹®ÀÚ¿­ 
		int pt = 0; 
		while (a[pt] != '*') {
			/*cout << a[pt] << ' '; */
			if (a[pt] != b[pt]) {
				cout << "NO"; 
				return 0; 
			}
			b[pt] = 'NULL';
			pt++; 
		}
		pt = 0; 
		while (a[a1 - 1 - pt] != '*') {
			if (a[a1 - 1 - pt] != b[b1 - 1 - pt]) {
				cout << "NO"; 
				return 0; 
			}
			pt++; 
		}
		cout << "YES"; 
		return 0; 
	}


	else if (!res) {
		int pt = 0;
		while (b[pt] != '*') {
			if (b[pt] != a[pt]) {
				cout << "NO";
				return 0;
			}
			a[pt] = 'NULL';
			pt++;
		}
		pt = 0;
		while (b[a1 - 1 - pt] != '*') {
			if (b[b1 - 1 - pt] != a[b1 - 1 - pt]) {
				cout << "NO";
				return 0; 
			}
			pt++;
		}
		cout << "YES";
		return 0;
	}
	return 0; 
}